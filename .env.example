# auto-daily 設定ファイル
# このファイルを .env にコピーして使用してください

# ===== LLM バックエンド設定 =====

# 使用する LLM バックエンド（デフォルト: ollama）
# 選択肢: ollama, openai, lm_studio
# AI_BACKEND=ollama

# ===== Ollama 設定 =====

# Ollama の接続先 URL（デフォルト: http://localhost:11434）
# OLLAMA_BASE_URL=http://localhost:11434

# 使用する Ollama モデル（デフォルト: llama3.2）
# OLLAMA_MODEL=llama3.2

# ===== OpenAI 設定 =====

# OpenAI API キー（AI_BACKEND=openai の場合は必須）
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx

# 使用する OpenAI モデル（デフォルト: gpt-4o-mini）
# OPENAI_MODEL=gpt-4o-mini

# ===== LM Studio 設定 =====

# LM Studio の接続先 URL（デフォルト: http://localhost:1234）
# LM_STUDIO_BASE_URL=http://localhost:1234

# 使用する LM Studio モデル（デフォルト: default）
# LM_STUDIO_MODEL=default

# ===== OCR 設定 =====

# 使用する OCR バックエンド（デフォルト: apple）
# 選択肢: apple（Vision Framework）, openai（GPT-4o Vision）, ollama（llava 等）
# OCR_BACKEND=apple

# OCR 用 Vision モデル（OCR_BACKEND が openai/ollama の場合）
# デフォルト: gpt-4o-mini（OpenAI）, llava（Ollama）
# OCR_MODEL=gpt-4o-mini

# OCR ノイズフィルタリング（デフォルト: true）
# true: メニューバー要素、API キー、ゴミ文字などを自動除去
# false: フィルタリングを無効化
# OCR_FILTER_NOISE=true

# ===== キャプチャ設定 =====

# 定期キャプチャの間隔（秒）（デフォルト: 30）
# AUTO_DAILY_CAPTURE_INTERVAL=30

# ===== ディレクトリ設定 =====

# ログ出力先ディレクトリ（デフォルト: ~/.auto-daily/logs/）
# AUTO_DAILY_LOG_DIR=/path/to/custom/logs

# 要約出力先ディレクトリ（デフォルト: ~/.auto-daily/summaries/）
# AUTO_DAILY_SUMMARIES_DIR=/path/to/custom/summaries

# 日報出力先ディレクトリ（デフォルト: ~/.auto-daily/reports/）
# プロジェクトルートに reports/ ディレクトリがある場合はそちらが優先されます
# AUTO_DAILY_REPORTS_DIR=/path/to/custom/reports
